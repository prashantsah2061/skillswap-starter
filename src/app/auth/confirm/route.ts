import { notImplemented } from "@/lib/api/not-implemented";
// TODO: verify email token_hash + allowed email OTP type; redirect to a fixed local path.
export async function GET(){return notImplemented("Email confirmation")}
